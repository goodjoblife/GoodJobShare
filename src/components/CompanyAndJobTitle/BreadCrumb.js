// @flow
import React from 'react';

type Props = {
  pageType: string,
  pageName: string,
  tabName: string,
};

const BreadCrumb = (props: Props) => <div>BreadCrumb</div>;

export default BreadCrumb;
